# Binaries
bin/
# Ad-hoc `go build -o <name>` artifacts at the repo root (the Taskfile builds
# under bin/; these guard against stray multi-MB binaries being staged).
/apiserver
/keleustesctl
/manager
*.exe
*.exe~
*.dll
*.so
*.dylib
Dockerfile.cross

# Test artifacts
*.test
*.out
coverage.out
coverage
cover*.out
*.coverprofile

# Go workspace
go.work
go.work.sum

# Build artifacts
dist/

# Generated Kubernetes files (keep vendored)
!vendor/**/zz_generated.*

# IDE
.idea/
.vscode/
*.swp
*.swo
.codex
.claude
.cursor/
.windsurf/
.continue/
.aider*

# OS
.DS_Store
Thumbs.db

# Local junk
.cache/
.gocache

# Design docs (local-only)
DESIGN.md

# Frontend
ui/node_modules/
ui/dist/
ui/.next/

# Graphify
.graphify_ast.json
.graphify_detect.json
.graphify_python
.graphify_chunks.json
.graphify_uncached.txt
# graphify-out/ is committed (GRAPH_REPORT.md is the canonical codebase map).
# Only per-machine working state is ignored.
graphify-out/cache/
graphify-out/cost.json
graphify-out/.rebuild.lock
graphify-out/.graphify_root
