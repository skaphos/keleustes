apiVersion: keleustes.skaphos.io/v1alpha1
kind: PromotionPolicy
metadata:
  name: prod-standard
spec:
  required:
    - imageSigned
    - sbomPresent
    - vulnThreshold
    - sourceHealthy
    - targetUnlocked
    - changeApproved
    - ownerApproved
