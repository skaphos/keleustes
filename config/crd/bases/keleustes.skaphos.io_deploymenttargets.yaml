---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: deploymenttargets.keleustes.skaphos.io
spec:
  group: keleustes.skaphos.io
  names:
    kind: DeploymentTarget
    listKind: DeploymentTargetList
    plural: deploymenttargets
    singular: deploymenttarget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .spec.cell
      name: Cell
      type: string
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.reachable
      name: Reachable
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeploymentTarget is a concrete place where an Application can
          run.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              DeploymentTargetSpec defines the desired state of DeploymentTarget.
              See PROPOSAL §8.5.
            properties:
              cell:
                description: Cell names the Cell this target belongs to.
                maxLength: 253
                type: string
              cluster:
                description: Cluster identifies the cluster Keleustes reconciles into.
                properties:
                  kubeconfigSecretRef:
                    description: |-
                      KubeconfigSecretRef references a Secret in the same namespace holding
                      the kubeconfig used to reach the cluster. When empty, Keleustes uses
                      its own service-account credentials.
                    properties:
                      name:
                        description: Name of the referenced object.
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  name:
                    description: Name is the cluster identifier as seen by Keleustes.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              environment:
                description: Environment names the Environment this target belongs
                  to.
                maxLength: 253
                minLength: 1
                type: string
              region:
                description: Region is the geographic or cloud-provider region.
                maxLength: 253
                type: string
            required:
            - cluster
            - environment
            type: object
          status:
            description: DeploymentTargetStatus defines the observed state of DeploymentTarget.
            properties:
              conditions:
                description: Conditions summarize the controller's view of the target.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastContactedAt:
                description: LastContactedAt is the last successful contact timestamp.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent metadata.generation
                  reconciled.
                format: int64
                type: integer
              reachable:
                description: |-
                  Reachable indicates whether Keleustes last contacted the target cluster
                  successfully.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
